#![allow(clippy::unnecessary_wraps)]

pub mod default {
    pub mod server_info;
    pub mod about;
    pub mod help;
}

pub mod user {
    pub mod online;
}

pub mod etc {
    pub mod test_command;
}

